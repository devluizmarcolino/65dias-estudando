// ! tipos de variáveis:
let idade = 19; // ! tipo number
let nome = "Luiz"; // ! tipo string
let logado = true; // ! tipo boolean

// * array e objetos:
let ingredientes = ["farinha", "água", "sal", "corante"]; // * array
let personagem = {
  nome: "Luizão",
  nivel: 33,
  forca: 103,
  magia: 113,
  vida: 10003,
  mana: 32341,
}; // * objeto

// ? function

// ? function somar(a, b) {
// ?  let resultado = a + b;
// ?  return resultado
// ? }   function normal

// ? let somar2 = (a, b) => {
// ?  let resultado = a + b;
// ?  return resultado
// ? } arrow function

// ? let x = somar(15, 50)
// ? let y = somar1(2, 8)
